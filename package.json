{
  "name": "launchkit",
  "version": "1.0.0",
  "private": true,
  "description": "AI website builder that ships real booking and payments, then launches on your own domain.",
  "type": "module",
  "main": "server.js",
  "engines": { "node": ">=20.6" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2"
  }
}
